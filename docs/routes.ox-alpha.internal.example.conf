# INTERNAL EVALUATION ONLY -- NOT A BETA OR PRODUCTION ROUTE.
#
# OpenRouter permission and Ox Alpha data-use terms remain unresolved. Read
# docs/providers/ox-alpha.md before doing anything with this file. Use only
# synthetic, non-sensitive prompts after the operator has confirmed that the
# intended measurement is permitted. Never commit OPENROUTER_API_KEY.
#
# This file proves the existing OpenAI-compatible adapter can describe the
# candidate route. Its presence does not authorize deployment.

# The short Osanwë-owned expiry is deliberate. Once it passes, the route is
# omitted from the catalog and refused before a token or budget is reserved.
# Re-review every policy source before choosing a new expiry.

# model             style   upstream                   credential        policy annotations
stealth/ox-alpha    openai  https://openrouter.ai/api  OPENROUTER_API_KEY retention=retained training=unknown provider_identity=undisclosed policy_source=https://openrouter.ai/stealth/ox-alpha policy_checked=2026-08-22 experimental=true expires=2026-08-29T00:00:00Z
