# Fail-closed route for the invited free beta. The project credential belongs
# only in GROQ_API_KEY on the gateway host; it must never enter this file, an
# enrollment package, the local client, or a release artifact.
#
# The Groq project is restricted separately to openai/gpt-oss-20b. Keeping the
# same exact allowlist here means either layer refuses an unexpected model.
# This temporary route expires automatically at the instant below. Set that
# boundary to no later than the API key's own expiry before deployment.
#
# model              style   upstream                      credential    lifecycle
openai/gpt-oss-20b    openai  https://api.groq.com/openai   GROQ_API_KEY  experimental=true expires=2026-09-04T23:59:59Z
